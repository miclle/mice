# Mice: Slider

'use strict';

(($) ->

  # TOOLTIP PUBLIC CLASS DEFINITION
  # ===============================
  class Slider
    constructor: (element, options) ->
      @slider
      @options

      @init()


    init: ->

  return
) jQuery